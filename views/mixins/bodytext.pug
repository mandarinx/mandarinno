mixin bodyTextH2(body)
  - var heading = body.blocks[0]
  case heading.type
    when 'heading1'
      - body.blocks.splice(0, 1)
      div
        h2
          != heading.text
        div
          != body.asHtml(ctx.linkResolver)
    default
      != body.asHtml(ctx.linkResolver)
